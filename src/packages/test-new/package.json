{
  "name": "@kibalabs/everypage-test-new",
  "version": "0.6.8",
  "private": true,
  "description": "",
  "scripts": {
    "build": "everypage build --clean --output-directory dist --build-hash testing123 --site-host test.evrpg.com && cp -R ./dist ./disttemp && mv ./disttemp ./dist/testing123",
    "publish": "aws s3 cp --recursive ./dist/ s3://public.kiba.dev/"
  },
  "dependencies": {
    "@kibalabs/everypage-new": "0.6.8"
  }
}
