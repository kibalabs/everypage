
export interface IWebsite {
  name: string;
  tagline: string;
  company: string;
  buildHash: string;
  version?: string;
  keywords?: [string];
  companyUrl?: string;
  title?: string;
  description?: string;
}
